# Workspace Rules

- **Deployment Constraint**: Do NOT automatically deploy to Vercel or push to GitHub unless the user explicitly asks for it. By default, only test and run changes locally (localhost).
